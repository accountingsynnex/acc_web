/* The one sign-in everybody in the team uses.

   ── HOW TO CHANGE THE PASSWORD ────────────────────────────────────────
   Open tools/set-password.html in a browser, type the new user name and
   password, and it prints the three lines below ready to paste. Then
   commit this file. There is nothing else to change and nobody has to
   clear their browser — every machine reads this file on load.

   ── WHAT THIS IS AND IS NOT ───────────────────────────────────────────
   This repository is public and the site is static, so treat this file as
   public knowledge: the password is not stored here, but the PBKDF2 hash
   of it is, and a short or guessable password can be worked back out of a
   hash offline by anyone who wants to. Someone can also simply type a
   page's URL and skip the sign-in entirely, because there is no server to
   stop them.

   So: pick a password used for NOTHING ELSE at the company, and read the
   sign-in as the lock on an office door — it keeps out whoever wanders
   past the browser, not whoever is after the numbers. */
window.AUTH_CONFIG = {
  // Shown on the sign-in form as the name to type.
  user: 'synnex',

  // PBKDF2-SHA256 of the password, 150,000 iterations, with the salt below.
  // Generated by tools/set-password.html — do not hand-edit.
  //
  // TEMPORARY, so the site is not locked while a real one is chosen. Replace
  // it: this password is written in the commit message that added it, which
  // makes it public knowledge and not a password at all.
  salt: '57cff18e3573c8fdaf482819f32a19fc',
  hash: '2f5eb82efe06b786d82d5e8c6929cd86733d118e6fc6a292b3f94b47061a6641',
  iter: 150000,
};
